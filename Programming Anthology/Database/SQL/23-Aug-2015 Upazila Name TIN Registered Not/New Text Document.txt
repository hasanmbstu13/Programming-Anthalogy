SELECT  DISTINCT UPZILA_NAME,Q25_IS_TIN_REGISTERED
FROM BBSEC2013_REPORTS
WHERE UPZILA_NAME NOT IN ( SELECT  UPZILA_NAME
FROM BBSEC2013_REPORTS
WHERE Q25_IS_TIN_REGISTERED = 1
GROUP BY UPZILA_NAME
HAVING COUNT(*) >= 0)
ORDER BY UPZILA_NAME;